---
name: svelte-component
description: Create Svelte 5 components using official MCP server workflow. Use when building any Svelte component, page, or UI element.
---

# Svelte Component Creation

1. Run `list-sections` via Svelte MCP → analyze `use_cases`
2. Fetch ALL relevant docs with `get-documentation`
3. Run `svelte-autofixer` (desired_svelte_version: 5), iterate until zero issues
